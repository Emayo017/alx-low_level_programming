#include <stdio.h>
/**
 * main - UPPER and lower case letter
 *
 * description - This is for UPPER and lower case letter
 * return (0);
 */
int main(void)
{
	char lowercase = 'a';
	char uppercase = 'A';

	for (letter <= 'z')
	putchar(lowercase);
		lowercase++
	for (uppercase <= 'Z')
	putchar(uppercase);
		uppercase++;
	putchar('\n');
	return (0);
}
